# Value Refinery

- Prototype: refinery.py
- Frozen copy: prototypes/refinery_singlefile_2026-01-15.py

Quickstart:
  source .venv/bin/activate
  python -m pip install -U pip
  python -m pip install -e ".[dev]"
  value-refinery --help

Next: we refactor legacy into core/ + packs/ once CLI runs.
